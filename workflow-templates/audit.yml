name: Audit

on: # yamllint disable-line rule:truthy
  push:
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    uses: theholocron/.github/.github/workflows/audit.yml@main
    secrets: inherit
